import React from 'react';
import { Box, Typography, Paper } from '@mui/material';

const AdminIam = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="h5" gutterBottom>Admin - IAM</Typography>
    <Paper sx={{ p: 2 }}>
      <Typography variant="body1">This is a placeholder IAM administration page.</Typography>
    </Paper>
  </Box>
);

export default AdminIam;
